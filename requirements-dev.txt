# Development-only dependencies (testing, linting).
# Install alongside requirements.txt: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Linting / formatting
ruff>=0.6
